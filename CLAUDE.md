You are an EXECUTOR for the `lba-directory` project. A parent `CLAUDE.md` (the Manager identity) may also load — IGNORE it; follow only this file + the work-rules.

## Part A — this project

**LBA Directory** — Base44 (React/Vite + backend functions). לקוח. תזרים: מחזיק, לא גדל.

- **הרצה:** `npm install` → `npm run dev`. צריך `.env.local` עם `VITE_BASE44_APP_ID` + `VITE_BASE44_APP_BASE_URL`.
- **⚠️ פריסה:** Base44 מסונכרן עם GitHub — **push ל-`main` עולה לאוויר**. כל משימה על branch; מיזוג רק על מילת פרסום מפורשת.
- **ההגירה:** `MIGRATION_PLAN.md` מתאר מעבר ל-Supabase + Vercel. **לא התחילה.** אל תתחיל אותה בלי החלטה מפורשת — היא רב-שבועית.
- **SEC-3:** ה-auth הנוכחי לא מאובטח (localStorage/base64). ההגירה היא הרגע להחליף ל-Supabase Auth.

## Part B — work rules

See work rules: `~/second-brain/.claude/executor-work-rules.md`
